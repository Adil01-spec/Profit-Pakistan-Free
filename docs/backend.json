
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user profile in the Profit Pakistan Pro application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "User's display name."
        },
        "photoURL": {
          "type": "string",
          "description": "URL of the user's profile photo, if available.",
          "format": "uri"
        }
      },
      "required": [
        "id"
      ]
    },
    "AnalysisRecord": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AnalysisRecord",
      "type": "object",
      "description": "Represents a single analysis record (either product launch plan or ad feasibility check) associated with a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AnalysisRecord entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N AnalysisRecord)"
        },
        "productName": {
          "type": "string",
          "description": "Name of the product being analyzed."
        },
        "category": {
          "type": "string",
          "description": "Category of the product."
        },
        "date": {
          "type": "string",
          "description": "Date and time when the analysis was performed.",
          "format": "date-time"
        },
        "type": {
          "type": "string",
          "description": "Type of analysis: 'Launch' or 'Feasibility'."
        },
        "results": {
          "type": "string",
          "description": "The JSON results of the analysis."
        }
      },
      "required": [
        "id",
        "userId",
        "productName",
        "category",
        "date",
        "type",
        "results"
      ]
    },
    "FreeUsage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FreeUsage",
      "type": "object",
      "description": "Tracks the free usage of report exports and AI prompts for a user within a 24-hour period.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Unique identifier of the user or device."
        },
        "lastReset": {
          "type": "string",
          "description": "Timestamp of the last usage counter reset.",
          "format": "date-time"
        },
        "exportsUsed": {
          "type": "number",
          "description": "Number of report exports used in the current period."
        },
        "aiPromptsUsed": {
          "type": "number",
          "description": "Number of AI prompts used in the current period."
        }
      },
      "required": [
        "userId",
        "lastReset",
        "exportsUsed",
        "aiPromptsUsed"
      ]
    },
    "Plan": {
      "title": "Plan",
      "description": "Describes a pricing plan for the application.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique ID for the plan (e.g., 'pro')." },
        "name": { "type": "string", "description": "Display name of the plan." },
        "price": { "type": "string", "description": "Price description (e.g., '₨ 799 /month')." },
        "highlight": { "type": "string", "description": "A short highlight of the plan." },
        "features": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of features included in the plan."
        }
      },
      "required": ["id", "name", "price", "features"]
    },
    "Ad": {
      "title": "Ad",
      "description": "Describes an ad banner configuration.",
      "type": "object",
      "properties": {
        "image": { "type": "string", "format": "uri", "description": "URL of the ad banner image." },
        "link": { "type": "string", "format": "uri", "description": "Destination URL for the ad." },
        "text": { "type": "string", "description": "Fallback text for the ad." }
      }
    },
    "PaymentRequest": {
        "title": "PaymentRequest",
        "description": "Represents a manual payment submission from a user for a Pro upgrade.",
        "type": "object",
        "properties": {
            "name": { "type": "string", "description": "Full name of the user." },
            "email": { "type": "string", "format": "email", "description": "Email address for account creation." },
            "phone": { "type": "string", "description": "Contact phone number." },
            "paymentMethod": { "type": "string", "enum": ["JazzCash", "EasyPaisa", "Bank Transfer"], "description": "The payment method used." },
            "transactionId": { "type": "string", "description": "The transaction or reference ID from the payment service." },
            "screenshotFile": { "type": "string", "description": "Filename of the uploaded screenshot." },
            "timestamp": { "type": "string", "format": "date-time", "description": "When the request was submitted." },
            "status": { "type": "string", "enum": ["pending", "verified", "rejected"], "description": "The verification status of the payment." }
        },
        "required": ["name", "email", "phone", "paymentMethod", "timestamp", "status"]
    }
  },
  "auth": {
    "providers": [
      "anonymous",
      "email"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores user profile information. Accessible only by the user."
    },
    "/users/{userId}/history/{recordId}": {
      "schema": { "$ref": "#/entities/AnalysisRecord" },
      "description": "Stores analysis records for each user."
    },
    "/free_usage/{userId}": {
      "schema": { "$ref": "#/entities/FreeUsage" },
      "description": "Tracks free usage for each user."
    },
    "/plans/{planId}": {
        "schema": { "$ref": "#/entities/Plan" },
        "description": "Stores details about the pricing plans, which are publicly readable."
    },
    "/ads/{adId}": {
        "schema": { "$ref": "#/entities/Ad" },
        "description": "Stores ad configuration data, which is publicly readable."
    },
    "/paymentRequests/{requestId}": {
      "schema": { "$ref": "#/entities/PaymentRequest" },
      "description": "Stores manual payment submissions for Pro upgrades. Writable by anyone, but not readable or updatable by clients."
    }
  }
}
